import 'package:flutter/material.dart';

class PickupOrderTile extends StatelessWidget {
  const PickupOrderTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
